{
  "name": "readme-stats-action",
  "description": "Text-based GitHub Readme Stats",
  "version": "0.1.0",
  "license": "MIT",
  "packageManager": "pnpm@8.12.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "vitest",
    "check": "biome check src",
    "build": "esbuild src/index.ts --platform=node --target=node24 --bundle --sourcemap --outfile=dist/index.js"
  },
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/github": "^9.1.1",
    "table": "^6.9.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.15",
    "@octokit/graphql-schema": "^15.26.1",
    "@types/node": "^25.9.1",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
