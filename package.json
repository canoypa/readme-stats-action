{
  "name": "readme-stats-action",
  "description": "Text-based GitHub Readme Stats",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "build": "esbuild src/index.ts --platform=node --target=node16 --bundle --minify --sourcemap --outfile=dist/index.js"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^5.1.1",
    "date-fns": "^2.30.0",
    "mdast-zone": "^6.0.1",
    "remark": "^15.0.1",
    "table": "^6.8.1"
  },
  "devDependencies": {
    "@octokit/graphql-schema": "^14.33.0",
    "@types/node": "^18.18.0",
    "esbuild": "^0.19.4",
    "typescript": "^5.2.2",
    "unified": "^11.0.3"
  }
}
